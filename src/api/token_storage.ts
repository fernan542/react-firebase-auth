// const key = '__auth_token__'

// export const readToken = (): string | null => sessionStorage.getItem(key)

// export const clearToken = (): void => sessionStorage.removeItem(key)

// export const saveToken = (value: string): void => sessionStorage.setItem(key, value)
